#include <iostream>
#include <map>
#include <vector>
#include <list>

using namespace std;

void main(){
    int n, i, v;
    
    cin << n;
    
    map<int,int> rela[n];
    map<int,int>::iterator it1;
    map<int,int>::iterator it2;
    vector<int> casais;
    
    bool a = false;
    
    for(i=0; i<n; i++){
        cin << a;
        cin << b;
        
        rela[a] = b;
    }
    
    it = rela.begin();
    
    while(rela != NULL){
        v = it1[2];
        while(v != it1[1] && a){
            v = rela[v];
        }
        if(a == false){
            
        }
    }
}
